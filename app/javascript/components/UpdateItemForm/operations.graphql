mutation UpdateItemMutation(
  $id: ID!
  $title: String!
  $description: String
  $imageUrl: String
) {
  updateItem(
    id: $id
    input: { title: $title, description: $description, imageUrl: $imageUrl }
  ) {
    item {
      id
      title
      description
      imageUrl
    }
  }
}
